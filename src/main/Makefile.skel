#
#  Makefile for GXemul src/main
#

CXXFLAGS=$(CXXWARNINGS) $(CXXOPTIM) $(CPEDANTIC) $(INCLUDE1)
LIBS=$(OTHERLIBS)

OBJS=Action.o ActionStack.o Checksum.o GXemul.o UnitTest.o

all: $(OBJS)

$(OBJS): Makefile


clean:
	rm -f $(OBJS) *core _test

clean_all: clean
	rm -f Makefile

